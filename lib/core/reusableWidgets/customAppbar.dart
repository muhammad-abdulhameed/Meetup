import 'package:evently/core/appMangers/imageManger.dart';
import 'package:flutter/material.dart';

 class CustomAppbar {
   static PreferredSizeWidget defaultAppBar = AppBar(

     title: Image.asset(ImageManger.appLogo),
   );


 }
